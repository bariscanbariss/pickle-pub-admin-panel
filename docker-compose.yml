version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: picklepub_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgrespassword
      POSTGRES_DB: picklepub
    ports:
      - "5432:5432"
    volumes:
      - picklepub_db_data:/var/lib/postgresql/data

volumes:
  picklepub_db_data:
